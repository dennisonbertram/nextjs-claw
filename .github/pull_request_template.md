## What this PR does

<!-- One paragraph summary. Link the issue if there is one. -->

## How I tested it

<!-- Be specific. Examples:
     - `bun run build` clean
     - `npx nextjs-claw scratch` then verified the chat panel boots
     - Posted a curl to /api/agent and confirmed the SSE stream
     - Attached a before/after screenshot for UI changes -->

## Checklist

- [ ] `bun run build` passes (in `template/`)
- [ ] No new runtime deps in the root `package.json` (CLI must stay zero-dep)
- [ ] If I added an npm dep to `template/package.json`, it's pinned exactly (no `^` / `~`)
- [ ] I read `CONTRIBUTING.md`
